import { Card, CardContent } from '@mui/material';
import React from 'react';

const Schedule = props => {
	return (
		<Card>
			<CardContent>thing</CardContent>
		</Card>
	);
};

export default Schedule;
