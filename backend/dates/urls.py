from django.urls import path, include
from . import views

urlpatterns = [
    # path('', None),
    # path('<int:date_id>/', None),
    # path('<int:date_id>/timeslots/', include('timeslots.urls')),
]
